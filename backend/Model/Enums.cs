namespace Program_Scheduling_Meruap.Models.Enums;

public enum TicketStatus
{
    Pending,
    Approved,
    Declined
}

public enum TicketType
{
    On,
    Off
}

public enum Sex
{
    Male,
    Female
}